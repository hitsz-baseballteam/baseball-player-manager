# PostgreSQL environment variable
DATABASE_URL=
# Optional custom CA PEM for non-public roots (`\n` escaped or multi-line).
DATABASE_CA_CERT=
# Generate with: npm run auth:env -- "your-passcode"
APP_ADMIN_PASSCODE_HASH=
AUTH_SECRET=
